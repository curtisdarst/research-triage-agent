# Pinned exactly. ADK ships breaking changes across major versions (see README),
# so this project tracks specific versions rather than ranges.
google-adk==2.6.3
google-genai==2.18.0
google-cloud-bigquery==3.43.0
db-dtypes==1.7.1
python-dotenv==1.2.2
feedparser==6.0.14
requests==2.34.2
pydantic==2.13.4
pyyaml==6.0.3
# Web deployment (Tier 2, Cloud Run). Already transitive deps of google-adk;
# pinned to the versions already resolved in this project rather than
# absolute latest, to avoid a resolver conflict with ADK's own constraints.
fastapi==0.139.2
uvicorn==0.51.0
